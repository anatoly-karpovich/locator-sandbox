export enum TaskGetByTopic {
  GET_BY_TEXT = "getByText",
  GET_BY_TITLE = "getByTitle",
  GET_BY_ROLE = "getByRole",
  GET_BY_PLACEHOLDER = "getByPlaceholder",
}

export enum TaskSection {
  LOCATORS = "locators",
  GET_BY = "getBy",
}

export enum Module {
  PLAYWRIGHT_LOCATORS = "locators",
}
